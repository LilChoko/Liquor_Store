import 'package:flutter/material.dart';

class Drink {
  String name;
  String conName;
  String backgroundImage;
  String cupImage;
  String description;
  Color lightColor;
  Color darkColor;

  Drink(this.name, this.conName, this.backgroundImage, this.cupImage,
      this.description, this.lightColor, this.darkColor);
}
